# --- OS & Editor Files (ไฟล์ขยะของระบบ) ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Dependencies (ไม่ต้องเก็บ Library เพราะ Install ใหม่ได้) ---
node_modules/
frontend/node_modules/
backend/vendor/

# --- Backend (Go) ---
# ไฟล์ Binary ที่ Compile แล้ว (ไม่ควรเก็บ)
*.exe
*.exe~
*.dll
*.so
*.dylib

# ไฟล์ Config ที่เก็บรหัสผ่าน Database (ห้ามหลุดเด็ดขาด!)
backend/db_config.json

# โฟลเดอร์ Build (ถ้ามี)
backend/build/

# ไฟล์ Binary (Linux/Mac)
backend/main
backend/backend

# --- Frontend (Next.js) ---
# โฟลเดอร์ Build ของ Next.js
frontend/.next/
frontend/out/
frontend/build/
frontend/dist/

# Environment Variables (เผื่ออนาคตมีการใช้)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
frontend/.env*

# --- Electron Builder Output ---
# ไฟล์ Installer (.exe) และไฟล์ขยะตอน Build Electron
frontend/dist/
frontend/release/

# --- Cypress / Testing (ถ้ามี) ---
frontend/cypress/videos
frontend/cypress/screenshots
frontend/coverage